// Header component for the patient booking system homepage.
// Displays the app title and a short description of the booking flow.

export default function Header() {
  return (
    <header className="header">
      <div>
        <h1>Patient Booking System</h1>
        <p>Choose a physician, select a time, and request an appointment.</p>
      </div>
    </header>
  );
}
